To build the site:

make html

or

sphinx-build -a -b html source build

To generate all the function docs automatically, use

sphinx-apidoc -o source/modules/ ../

and change the title in modules.rst

The “notebook.rst" pages must be modified prior to compilation:
- python blocks must be removed in favor of 

blablabla:
::
   >>> some code …
   results

- images must be centered by adding

    :align: center

right after the input tag